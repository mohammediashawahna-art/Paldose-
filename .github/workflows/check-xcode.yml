name: Check Xcode

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: macos-15

    steps:
      - name: Check Xcode
        run: |
          xcodebuild -version
          xcode-select -p
          security find-identity -v -p codesigning || true
